name: dispatch-timing-noop
on:
  push:
    branches:
      - 'workflow/dispatch-timing/run-**'
    paths:
      - 'runs/payloads/**'
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "noop"
